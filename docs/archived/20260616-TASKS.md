# 任务执行文档 — Assign 工作流 v2（Phase 16–19）

> **归档日期**：2026-06-16  
> **状态**：✅ 全部交付（验收 11/11）  
> **设计依据**：[trigger-rules-and-workflow-improvement.md](20260615-trigger-rules-and-workflow-improvement.md)  
> **完成总览**：[assign-workflow-progress.md](20260616-assign-workflow-progress.md)

---

## 阶段概要

| 阶段 | 代号 | 交付物 | 状态 |
|------|------|--------|------|
| Phase 16 | P0 | Assign 主路径 + 数据模型 + L1 门禁 | ✅ |
| Phase 17 | P1 | Session 续作 + WorkflowPolicy L2/L3 | ✅ |
| Phase 18 | P2 | 生命周期 + Web UI + routes 移除 | ✅ |
| Phase 19 | P3 | 运维 reset + Agent 模板 + 文档 | ✅ 核心 |

**原则**：Assign 触发 Who，WorkflowContext 定义 When，WorkflowPolicy 定义能不能转，AgentSession 支撑 Continue。v2 不兼容 Label 阶段触发。

---

## Phase 16（P0）：状态机与 Assign 主路径 ✅

**交付标准**：Issue Assign 功能性 Agent → 按 role 入队；PR Request Reviewer → review；L1 违规有评论拒绝；无自触发、无同 issue 并发双 Task；Label / labeled 不触发 Task。

### 16.1 数据模型与迁移

- [x] `agents.role`：`analyze` | `coder` | `review`
- [x] `workflow_contexts` 表 + 唯一索引 `(repo, issue_id)`
- [x] `agent_sessions` 表
- [x] `tasks.session_id`, `tasks.role`
- [x] `internal/store` CRUD + 单元测试
- [x] API：`GET/PUT` Agent 含 `role`

### 16.2 Agent 注册与 API / UI

- [x] `CreateAgentRequest` / `Agent` 增加 `role`
- [x] Web UI role 字段与徽章
- [x] README / 文档 Assign 说明

### 16.3 Event Resolver

- [x] `EventResolver` + `SetWorkflowComponents` v2 流水线
- [x] `issues.assigned` 仅本次 `assignee.login`
- [x] `agent.role` → task_type；`bug` → `fix_bug`
- [x] PR `review_requested` + linked Issue
- [x] `issues.unassigned` 忽略
- [x] L1 + L2 + Session + in-flight + 进度评论
- [x] Label 触发移除；`determineTaskType` label 分支删除

### 16.4 WorkflowContext 状态机

- [x] `Transition` / `ApplyTransition`
- [x] stage：`idle | analyzing | analyzed | developing | reviewing | done`
- [x] Task 成功回调；PR merged / Issue closed → done

### 16.5 L1 结构性门禁

- [x] `gate_l1.go`：review 需 PR、closed PR 拒绝
- [x] `postGateComment` + `<!-- gateway-agent -->`

### 16.6 并发与自触发防护

- [x] sender 过滤 + in-flight 锁 + pending Task 去重

### 16.7 集成测试

- [x] Assign analyze/coder、unknown、self-trigger、review、L1、labeled、bug 共 8 项

### 16.8 弃用 Label 触发（Breaking）

- [x] `router.go` / `route.go` 删除；`DROP TABLE routes`
- [x] CHANGELOG Breaking

### 16.9 文档

- [x] `config.example.yaml` workflow/session 注释
- [x] CHANGELOG Phase 16

---

## Phase 17（P1）：Session 续作与 WorkflowPolicy ✅

**交付标准**：coder 不删 workspace；@mention 续作；L2 可配置；进度/完成评论；L3 建议。

### 17.1–17.6

- [x] `SessionService`：GetOrCreate、CompleteTask、Archive
- [x] DevRunner Session 级 Workspace，coder 不 Cleanup
- [x] @mention + `/dev` `/reply` `/force`
- [x] `policy.go`：free/standard/strict + EvaluateGate + L3
- [x] 测试：session(7) + policy(18) + mention(10) + 集成 4 项

---

## Phase 18（P2）：生命周期、Web UI、routes 移除 ✅

**交付标准**：closed/merged 归档；TTL；role UI；routes 完全移除。

### 18.1–18.6

- [x] `lifecycle.go`：closed/merged、10min cleanup loop
- [x] `EnforceDiskLimit` + LRU
- [x] Agents/AgentDetail role；TriggerRules 路由删除；routes Tab 弃用提示
- [x] routes API/表/代码移除
- [⏭️] 18.5 Gitea unassign（已跳过）
- [x] `TestIssueClosedArchivesSession` + lifecycle 单元测试

---

## Phase 19（P3）：增强与运维 ✅ 核心

**交付标准**：reset；Agent 模板创建；文档同步。

### 19.1–19.4

- [x] `/gateway reset` + `POST /api/sessions/reset`
- [x] Agents.vue：role + Prompt 模板 + repos
- [x] Agent repos 关联 + 协作者警告
- [x] ARCHITECTURE、README、Dashboard、§11.2 迁移对照

---

## 验收清单（11/11）✅

- [x] Assign analyze-* → analyzed
- [x] Assign coder-* → PR + Session workspace
- [x] bug 标签 → fix_bug
- [x] PR Request reviewer-* → reviewing
- [x] @coder PR 评论续作
- [x] standard/strict 门禁
- [x] 无 PR review → L1 拒绝
- [x] Agent 评论不循环
- [x] PR merge workspace 回收
- [x] labeled 不产生 Task
- [x] routes API/UI 已移除

---

## 执行顺序（归档）

```
Phase 16.1 → 16.2 → 16.3+16.8 → 16.4 → 16.5 → 16.6 → 16.7 → 16.9
    ↓
Phase 17.1 → 17.2 → 17.3 → 17.4 → 17.5 → 17.6
    ↓
Phase 18.1 → 18.2 → 18.3 → 18.4 → 18.6
    ↓
Phase 19.x
```
