# 任务清单（Phase 1 + Phase 2 已完成部分，已归档）

> **状态：已归档（2026-08-14）**  
> 现行任务清单见 [`../TASKS.md`](../TASKS.md)（仅保留未完成待办与新增规划）。  
> 本文归档 **Phase 1（1.1–1.6）** 与 **Phase 2 已完成部分（2.0、2.1、2.2、2.3.3、2.3.4、2.4.3）** 的完整交付记录。  
> 来源：[`docs/TASKS.md`](../TASKS.md) 于 2026-08-14 的状态。

---

> 更新：2026-08-14（Phase 1 全部收官；Phase 2 主链路已落地并通过全量 E2E）  
> 产品边界：**Gitea 优先** · 内置 Agent 默认可用 · **可插拔 harness 执行内核**（builtin / OpenCode / Hermes / Phase 3 的 Pi·Codex·Claude） · Matea 是 Gitea 唯一写方 · 不自研 IM SDK · 不引入外部 harness SDK  
> 决策：
> - [matea_产品演进实施计划_保留产品形态_引入_hub_后端.md](../matea_产品演进实施计划_保留产品形态_引入_hub_后端.md)
> - [20260805-Phase2-plan.md](20260805-Phase2-plan.md)
> - 已归档 v0.11.4 任务清单 → [20260803-TASKS.md](20260803-TASKS.md)

---

## 演进主线（本阶段）

```text
P0–P2 → P3 → 写路径/摩擦/Bootstrap（已归档）→ PR 续作注入 review / 逻辑 Issue 归一 / Agent 并发 / 可观测性（已交付）
        │
        ├─► Phase 1：HubBackend 抽象 + Gitea 触发入口整理 + 体验简化 ✅
        ├─► Phase 2：大脑可插拔地基（Harness+ToolBox）→ Hub 后端接入（OpenCode 全 role / Hermes）→ deliver 出站 ✅
        └─► Phase 3：多触发形态（REST/CLI）+ 策略配置化（待办）
```

---

## Phase 1：稳住产品 + 关键抽象（已收官）

目标：不改动业务行为，为 Hub 后端演进打好接口；默认体验仍是「下载二进制 → 3 步可用 → Gitea @ 三 Agent」。

### 1.1 Agent 配置简化：role 闭集 + 三 Agent 默认模板

> 边界：**`role` 为闭集** `analyze|coder|review`（创建任何 Agent 三选一）；**Agent 实例数不限**——同一 role 可创建多个实例按仓库/技术栈特化（如 `matea-coder-go` / `matea-coder-fe`）；三个模板仅是开箱默认值，不是数量上限。

- [x] **1.1.1** 明确 `role` 为 Agent 职责的唯一真相（现状已满足，仅补文档）  
  在 ARCHITECTURE.md / README.md 显式声明：`role` 为闭集 `analyze|coder|review`，同一 role 允许多 Agent 实例（按仓库/技术栈特化）；**不引入 capabilities 概念**。  
  ✅ 已完成（提交 91251ef）

- [x] **1.1.2** 改造现有 `applyRoleWizard` 向导为默认模板：`matea-analyst` / `matea-coder` / `matea-review`  
  命名 `code-*` → `matea-*`、补默认 backend（`builtin`）、配合 1.1.3 填 gitea_username。默认 backend 统一为 `builtin`（决策 #13）。  
  ✅ 已完成（提交 40c7454, 5a49966）

- [x] **1.1.3** Agent 名自动映射 Gitea username（含账号保护）  
  `matea-analyst` → `@matea-analyst`；托管标记 + 冲突报错 + `gitea.auto_provision: false` 时不触碰 Gitea。  
  ✅ 已完成（提交 f00c998）

- [x] **1.1.4** 合并 Assign 与 @mention 触发语义（保留 surface 维度，拆三步）  
  - [x] 1.1.4a 抽取 `(role, surface, intent)` 解析表，行为与现状完全等价  
  - [x] 1.1.4b 统一 Intent 命名与用户可见文案为「拉 Agent 进入会话」  
  - [x] 1.1.4c 补齐 Assign 分支能力缺口：`ResolveLogicIssueAndPR`、Fixes 解析、`/force` 支持、斜杠命令行首锚定  
  ✅ 已完成（提交 fa5d498, 1d05702, d19a80f）

### 1.2 抽象：HubBackend 接口

- [x] **1.2.1** 在 `internal/agents` 定义 `HubBackend` 接口  
  `Name() / Submit(ctx, TaskContext) → Handle / Poll(ctx, Handle) / Cancel(ctx, Handle) / Capabilities() / HealthCheck()`；`Handle` 随任务持久化到 SQLite，支持重启后恢复长任务、防重复提交。  
  ✅ 已完成（提交 ede10da）

- [x] **1.2.2** 定义 `TaskContext` / `BackendResult` / `GiteaAction` / `DeliverRequest` 类型  
  覆盖全 task_type；预留 `MemoryKeys`、`Channel`、`ThreadID`。  
  ✅ 已完成（提交 ede10da）

- [x] **1.2.3** 把现有 `internal/agent` 的 loop 封装为 `builtin` backend  
  不废弃 `internal/llm` 和内置 Agent Loop；`RunnerFactory` 通过 `backend` 名选择。  
  ✅ 已完成（提交 fc0552b）

- [x] **1.2.4** 在四个 Runner 中预留 `hub-*` backend 分流分支  
  `hub-hermes` / `hub-openclaw` / `hub-api` 返回明确错误；`hub-opencode` 保持可用。未知 backend 报错，不静默回落 builtin。  
  ✅ 已完成（提交 75d481c）

- [x] **1.2.5** 将现有 `CodingBackend`（OpenCode）改造为 `hub-opencode` 可选实现  
  与 `HubBackend` 接口对齐；Phase 1 保持 OpenCode 可用。  
  ✅ 已完成（提交 f783e43）

- [x] **1.2.6** backend 标识符迁移：`internal` → `builtin`、`opencode_http` → `hub-opencode`  
  新增 `config.NormalizeBackend`；源码、配置、测试、DB、UI 全部收敛；DB 一次性迁移；旧标识符兼容。  
  ✅ 已完成（提交 64a8b42, 3a0f08e）

- [x] **1.2.7** Mock Hub 测试地基  
  TestEnv 增加 Mock Hub（正常返回 / 超时 / 502 / 鉴权失败 / 异步长任务 / 取消 / 未知任务）。  
  ✅ 已完成（提交 c13addb）

### 1.3 触发入口整理：`internal/ingress/gitea`

- [x] **1.3.1** 将 `internal/webhook` 事件解析逻辑迁到 `internal/ingress/gitea`  
  统一输出 `Intent` 结构。  
  ✅ 已完成（提交 24dd3a1, 861f59c）

- [x] **1.3.2** `Intent` 结构预留 `Source` / `Channel` / `ThreadID` 字段  
  为 MCP/API/CLI 触发留扩展。  
  ✅ 已完成（提交 bf96d5b）

- [x] **1.3.3** 不新增其他触发器实现  
  Phase 1 只聚焦 Gitea webhook；接口先设计好。  
  ✅ 已完成（无需代码）

### 1.4 LLM 配置边界 + UI 动态表单

- [x] **1.4.1** 明确 `llm.providers` 仅用于 `builtin` backend  
  Hub backend 的 LLM 配置由 Hub 自己管理。  
  ✅ 已完成（schema.go 注释 + config.full-example.yaml 注释）

- [x] **1.4.2** Agent 编辑页按 `backend` 动态显示字段  
  `builtin`：Provider/Model/Temperature/Loop Config；`hub-opencode`：仅 `opencode_model/provider/agent`；`hub-hermes`：URL/Skill/API Key/Memory Keys；System Prompt / User Template 所有后端均显示。  
  ✅ 已完成

- [x] **1.4.3** 系统配置页提示「LLM Providers 仅用于 builtin Agent」  
  SystemConfig.vue LLM 配置 Tab 顶部 alert 明确说明。  
  ✅ 已完成

### 1.5 工作流与体验

- [x] **1.5.1** 保留 `free/standard/strict` 预设，不开放策略单元  
  gate 评估拆成独立函数，便于 Phase 3 配置化。  
  ✅ 已核验（`internal/workflow/policy.go`）

- [x] **1.5.2** 隐藏 WorkflowContext stage 的用户可见面  
  用户只感知「Agent 正在处理 / 已回复 / 已创建 PR」；修复 mention 路径 stage 滞留。  
  - [x] 1.5.2A 后端 stage 回落（修复滞留）  
  - [x] 1.5.2B 前端语义化  
  ✅ 已完成

- [x] **1.5.3** 保留 `EnsureGiteaAccount`，默认开启，可配置关闭  
  `gitea.auto_provision: false` 时仅校验/提示手动创建。  
  ✅ 已交付

### 1.6 文档与引导

- [x] **1.6.1** 更新 README / 快速开始：默认体验仍是「下载二进制 → 配 Gitea/LLM → Gitea @matea-analyst」  
  ✅ 已完成

- [x] **1.6.2** 新增「接入 Hub 后端」进阶章节，不放在快速开始里  
  ✅ 已完成

- [x] **1.6.3** 更新 AGENTS.md 产品叙事：默认 builtin，可选 hub-*  
  ✅ 已完成

---

## Phase 2：Hub 后端接入（已完成部分）

> 方案：[20260805-Phase2-plan.md](20260805-Phase2-plan.md)（D1–D12 已拍板，方案冻结）  
> 关键收束：D2 完成通知 = Poll；D4 MCP Server 降级可选；D5 deliver 仅出站；2.1.5 删除 MCP 操作 Matea 沙箱；D10/D11/D12 为大脑可插拔三支柱。

### 2.0 机制骨架：大脑可插拔地基

- [x] **2.0.1** 抽出统一 `Harness` 接口 + `harnessRouter` 注册表（D10）  
  `HarnessTurnInput/Result`、`Profile`、`controlTransport`/`toolTransport` 元数据；注册表 `Map<HarnessID, Harness>`。  
  ⚠️ 诚实更正（2026-08-11）：接口与注册表已建，但尚未收敛三套执行接口——仍属「接口就绪、未接线」基础设施，收敛留待 D12 / Phase 3。  
  ✅ 已完成（提交 b4aab51）

- [x] **2.0.2** 现有 `builtin` 改造为 in-process adapter（D10 实证 1）  
  内部仍跑现有多 role Agent Loop，仅套上 `Harness` 接口。  
  ✅ 已完成（提交 b4aab51）

- [x] **2.0.3** 定义 `ToolContext`/`ToolBox` 并按三层策略暴露（D11）  
  沙箱类仅 builtin；Gitea 类 Phase 2 只做读侧；远程 harness ToolBox 只能 append 不能 replace。  
  ⚠️ 诚实更正（2026-08-11）：`NewToolBox` 全仓零非测试引用——属悬空基础设施，待 D12 / Phase 3 接线。  
  ✅ 已完成（提交 b4aab51）

- [x] **2.0.4** 网关级 skill 经 ToolBox 暴露（D11）  
  网关级 skill body 作为 system prompt 片段；script 工具在 Matea 侧沙箱内执行。  
  ✅ 已完成（提交 b4aab51）

- [x] **2.0.5** 配置新增 `workspace_transport` 语义位（D11 部署档位）  
  `shared_path`（默认，Phase 2 唯一实现）｜ `mcp`（Phase 3）。  
  ✅ 已完成（提交 b4aab51）

### 2.1 hub-hermes 实现（基于官方 Runs API）

- [x] **2.1.1** 实现 `internal/agents/backends/hermes`  
  `Submit` → `POST /v1/runs`，`Poll` → `GET /v1/runs/{run_id}`，Bearer 鉴权，`session_id` 同 repo 多任务续接。  
  - [x] 2.1.1-a **Handle 持久化 + Executor 重启拾取**：`hub_handles` 表 + `SaveHubHandle` + `ReattachHubHandles`。  
  - [x] 2.1.1-b **IdempotencyKey 去重**：提交前 `GetHubHandle` 命中非终结 Handle 即复用。  
  ✅ 已完成（提交 224e7ab）

- [x] **2.1.2** `analyze_issue` → Hermes  
  ✅ 已完成（e2e_test.go::TestAnalyzeRunnerViaHermes PASS）

- [x] **2.1.3** `review_pr` → Hermes  
  ✅ 已完成（e2e_test.go::TestReviewRunnerViaHermes PASS）

- [x] **2.1.4** `reply_comment` → Hermes  
  ✅ 已完成（e2e_test.go::TestInteractionRunnerViaHermes PASS）

- [x] **2.1.5** 验证同一 repo 上 analyze → review → code 的记忆共享（D3）  
  新增 `memories` 表（repo/issue 级 KV）+ `MemoryKeys` 读写；Hermes 侧用 `session_id` 续接同一会话上下文。  
  ⚠️ 原「Hermes 经 MCP 操作 Matea 沙箱」已删除。  
  ✅ 已完成（e2e_test.go::TestHermesMemorySharing PASS）

### 2.2 hub-opencode 改造（D7 三刀）

- [x] **2.2.1** `AnalyzeRunner` 后端感知 + analyze 带工作区给 OpenCode  
  ✅ 已完成（含 `prepareAnalyzeWorkspace`、OpenCode 目录绑定、失败降级）

- [x] **2.2.2** review 带工作区给 OpenCode  
  ✅ 已完成（含 `prepareReviewWorkspace`、`saveReviewMemory`、PR head clone）

- [x] **2.2.3** reply 全 role（对话类，单轮，工作区非必需）  
  ✅ 已完成（含 `prepareReplyWorkspace` 最小空 workspace、失败降级）

- [x] **2.2.4** OpenCode 无 IM 渠道时，配置 `deliver.webhook_url` 回传  
  ✅ 已完成（config.full-example.yaml + README + AGENTS.md + DEPLOYMENT.md 文档说明）

### 2.3 MCP Server + Deliver（已完成部分）

- [x] **2.3.3** 实现 `deliver` 模块：仅出站事件扇出  
  `event/channel/thread_id/repo/issue_id/pr_id/action/content` → POST `deliver.webhook_url`；空 URL = no-op；2xx 成功；5xx/网络错误按 max_retries 退避重试；4xx 不重试。  
  ✅ 已完成（含 `deliver_test.go` + `agents/deliver_wiring_test.go` 全 PASS）

- [x] **2.3.4** 配置 `deliver.webhook_url`：可指向用户自建 IM bridge 或 Hub 接收端  
  `SystemConfig.vue` 新增「Deliver」Tab（webhook_url / timeout / max_retries），后端 `keys.go` 白名单已含，可持久化并热更新。  
  ✅ 已完成（2026-08-12，S2）

### 2.4 验证场景（已完成部分）

- [x] **2.4.3** PR 合并后，Matea deliver 出站事件 → 用户飞书（经 outbound deliver 或 Hermes gateway `deliver` 回传）  
  `dispatcher.handleLifecycleEvent` 在 `OnPRClosed(merged=true)` 后经 `emitPrMerged` 发出 `pr_merged` 事件；未配置 `webhook_url` 静默 no-op。跨系统「Hermes gateway → 飞书」腿仍需真实 Hermes 实例人工验收。  
  ✅ 已完成（2026-08-13）

---

## 全量验证记录

| 维度 | 时间 | 结果 | 报告 |
|---|---|---|---|
| 后端/API E2E | 2026-08-13 | 10/10 PASS | [20260813-Phase2-e2e-report.md](20260813-Phase2-e2e-report.md) |
| WebUI E2E（Playwright） | 2026-08-13 ~ 14 | 6/6 PASS | [20260814-Phase2-webui-e2e.md](20260814-Phase2-webui-e2e.md) |
| 全量回归 | 2026-08-13 | 17/17 包 PASS | 见上述 E2E 报告 |

---

## 后续待办

见现行清单 [`../TASKS.md`](../TASKS.md)。
